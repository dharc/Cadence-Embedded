SHELL=/bin/sh

CC=@CC@
AR=ar
CPP=@CXX@
CPPFLAGS=@CXXFLAGS@
LDFLAGS=@LDFLAGS@
INCLUDES=-I@top_srcdir@/libcadence-embedded/include -I@top_builddir@/libcadence-embedded/include

SOURCES=test.cpp
OBJECTS=$(SOURCES:.cpp=.o)
LIB=libcadencetest.a

all: $(SOURCES) $(LIB)

$(LIB): $(OBJECTS)
	$(AR) cq $@ $(OBJECTS)

.cpp.o:
	$(CPP) -fPIC -c $(CPPFLAGS) $(INCLUDES) $< -o $@

clean:
	rm -f *.o
	rm -f *.a
