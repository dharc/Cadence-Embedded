this.ui = (new
	browser = (new
		root = (@root.notations.dasm.variables)
		useid = true
		hideid = true
		hideparent = false
		hidekey = true
		refreshrate = 50
	)
);

@ui = (this.ui);

this.modules.ui = (new type=Module
	file = (new type=LocalFile
		filename = "libcadence-vm_ui.so"
	)
	base = (this)
	update = true
	frequency = 0.00
);

